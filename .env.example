# GitHub Activity section — read server-side only, GITHUB_TOKEN is never sent to the browser.
GITHUB_USERNAME=
GITHUB_TOKEN=

# Base URL of the backend API used by the contact form.
NEXT_PUBLIC_API_URL=http://localhost:4000
